"""
Experiment 7 model: ACT + CVAE — forked road.

Assembles:
    shared.models.cnn_backbone  (4-conv variant)
    shared.models.transformer
    shared.models.cvae
    shared.models.mlp_head

CVAE models ambiguity from two valid paths at the junction.
"""
